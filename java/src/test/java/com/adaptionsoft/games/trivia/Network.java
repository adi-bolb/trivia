package com.adaptionsoft.games.trivia;

/**
 * Created by adi on 12/5/14.
 */
public class Network {
    static boolean networkConnect(boolean network) {
        return !network;
    }

    private static boolean networkSendMessage(boolean network) {
        return !network;
    }

    private static boolean networkReceiveMessage(boolean network) {
        return !network;
    }
}
